import { abilityModifier } from ".";

const playerStats = ((str, dex, con) => {

    const strength = str;
    const dexterity = dex;
    const constitution = con;

    function meleeAttack(){
        
    }

    return {

    }

});

